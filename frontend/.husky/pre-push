# .husky/pre-commit

if git diff --name-only "$(git merge-base HEAD @{upstream})" HEAD | grep -q '^frontend'; then
  echo "🔍 Changes detected in the conversation module..."
#  cd frontend && pnpm run pre-push && pnpm run build
fi